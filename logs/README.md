# Logs

Os testes de segurança podem gerar `security_test_results.jsonl` nesta pasta.

Nesta versão, não há log final de uma execução completa porque a execução depende de chamadas de API. O código dos testes está presente em `tests/test_security.py`.
