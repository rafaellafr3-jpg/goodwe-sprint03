# Resultados

Esta pasta é destinada aos resultados gerados pelos experimentos.

## Estado atual

Os scripts de comparação estão prontos, mas não há resultados quantitativos finais versionados porque a execução depende de credenciais/créditos de API. Não foram criados resultados fictícios.

## Quando houver credenciais

Execute:

```bash
python tests/run_model_comparison.py
```

O script gera o arquivo `results/model_comparison_raw.json`, que pode ser analisado e documentado em `docs/relatorio_modelos.md`.
